<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.healthcaredaas.data.cloud.foundation.mgmt.dao.ApplicationDao">
    <select id="selectUserApps" resultType="cn.healthcaredaas.data.cloud.foundation.mgmt.model.Application">
        select a.* from mgmt_application a
        <if test="userId!=null">
            inner join rbac_role_authority rra on rra.authority_id = ra.id
            inner join rbac_role rr on rr.role_id = r.id
            inner join rbac_user_role rur on rr.id = ur.role_Id
        </if>
        <where>
            a.parent_id = #{appId}
            and a.status = 1
            <if test="userId!=null">
                and rra.authority_type = #{type}
                and rur.user_id = #{userId}
            </if>
            <if test="isMicro!=null">
                and a.is_micro_app = #{isMicro}
            </if>
        </where>
        order by a.order_no,a.app_code
    </select>
</mapper>